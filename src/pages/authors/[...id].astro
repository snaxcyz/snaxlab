---
import { getAllAuthors } from '@/lib/data-utils'

export async function getStaticPaths() {
  const authors = await getAllAuthors()
  return [
    ...authors.map((author) => ({ params: { id: author.id } })),
    { params: { id: 'enscribe' } },
  ]
}

return Astro.redirect('/author', 301)
---
